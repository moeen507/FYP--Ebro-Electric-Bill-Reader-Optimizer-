import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF009688); // Teal
  static const Color secondary = Color(0xFF263238); // Blue Grey (Dark)
  static const Color background = Color(0xFF1A222C); // Very dark blue
  static const Color card = Color(0xFF232F3E); // Slightly lighter dark
  static const Color accent = Color(0xFFFFC107); // Amber for highlights
  static const Color text = Color(0xFFECEFF1); // Light grey for main text
  static const Color textSecondary =
      Color(0xFFB0BEC5); // Muted grey for secondary text
  static const Color error = Color(0xFFD32F2F); // Red for errors
}
